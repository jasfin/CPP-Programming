#include<iostream>
#include<set>
using namespace std;
int main()
{
    set<int> s;
    set<int>::iterator it;
    s.insert(10);
    s.insert(20);
    s.insert(10);
    s.insert(5);
    s.erase(5);
    cout<<"\n size:"<<s.size();
   // cout<<"find result"<<s.find(20); error
    for(it=s.begin();it!=s.end();it++)
       cout<<"\t"<<*it;
     // cout<<"\t"<<s[i];   
}